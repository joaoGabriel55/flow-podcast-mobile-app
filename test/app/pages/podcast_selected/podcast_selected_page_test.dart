import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('PodcastSelectedPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PodcastSelectedPage(title: 'PodcastSelected')));
    //  final titleFinder = find.text('PodcastSelected');
    //  expect(titleFinder, findsOneWidget);
  });
}
