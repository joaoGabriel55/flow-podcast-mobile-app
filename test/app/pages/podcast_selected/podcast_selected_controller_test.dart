import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:flowpdc_app/app/pages/podcast_selected/podcast_selected_controller.dart';
import 'package:flowpdc_app/app/app_module.dart';

void main() {
  initModule(AppModule());
  // PodcastSelectedController podcastselected;
  //
  setUp(() {
    //     podcastselected = AppModule.to.get<PodcastSelectedController>();
  });

  group('PodcastSelectedController Test', () {
    //   test("First Test", () {
    //     expect(podcastselected, isInstanceOf<PodcastSelectedController>());
    //   });

    //   test("Set Value", () {
    //     expect(podcastselected.value, equals(0));
    //     podcastselected.increment();
    //     expect(podcastselected.value, equals(1));
    //   });
  });
}
