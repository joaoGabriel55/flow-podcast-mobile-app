

void main() {
//  testWidgets('PodcastCardWidget has message', (tester) async {
//    await tester.pumpWidget(buildTestableWidget(PodcastCardWidget()));
//    final textFinder = find.text('PodcastCard');
//    expect(textFinder, findsOneWidget);
//  });
}
