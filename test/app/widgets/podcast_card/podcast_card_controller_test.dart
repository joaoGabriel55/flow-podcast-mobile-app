import 'package:flowpdc_app/app/app_module.dart';
import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  initModule(AppModule());
  // PodcastCardController podcastcard;
  //
  setUp(() {
    //     podcastcard = AppModule.to.get<PodcastCardController>();
  });

  group('PodcastCardController Test', () {
    //   test("First Test", () {
    //     expect(podcastcard, isInstanceOf<PodcastCardController>());
    //   });

    //   test("Set Value", () {
    //     expect(podcastcard.value, equals(0));
    //     podcastcard.increment();
    //     expect(podcastcard.value, equals(1));
    //   });
  });
}
